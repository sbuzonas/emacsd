;;** 15 (info "(emacs)Rectangles")

(when section-rectangles (message "15 Rectangles...")

;; `kill-rectangle' (C-x r k) and `yank-rectangle' (C-x r y) can
;; be very
;; useful for shifting cells up/down within a column while
;; leaving remaining
;; columns intact.

(message "15 Rectangles... Done"))
